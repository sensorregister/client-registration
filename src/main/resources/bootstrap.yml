---

spring:
  application:
    name: registration

feign:
  hystrix:
    enabled: false

---

spring:
  profiles: dev

server:
   port: 8082

eureka:
  client:
    enabled: false

register:
   ribbon:
      listOfServers: http://localhost:8081
